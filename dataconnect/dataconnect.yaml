specVersion: "v1"
serviceId: "agent"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "agent-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
